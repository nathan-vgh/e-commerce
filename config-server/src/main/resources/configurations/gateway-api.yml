server:
  port: 8080

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: a-service
              uri: http://localhost:8081
              predicates:
                - Path=/a-service/**
              filters:
                - name: CircuitBreaker
                  args:
                    name: aServiceCircuitBreaker
                    fallbackUri: forward:/fallback/default
            - id: b-service
              uri: http://localhost:8082
              predicates:
                - Path=/b-service/**
              filters:
                - name: CircuitBreaker
                  args:
                    name: bServiceCircuitBreaker
                    fallbackUri: forward:/fallback/default
          discovery:
            locator:
              enabled: true
